% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_trip_start_time.R
\name{optimize_trip_start_time}
\alias{optimize_trip_start_time}
\title{Optimize trip start times to maximize hourly earnings}
\usage{
optimize_trip_start_time(
  trips_dt,
  start_day_fitted_model,
  valid_start_times_dt
)
}
\arguments{
\item{trips_dt}{A data.table containing trip information. Must have columns:
\code{hvfhs_license_num}, \code{request_datetime} (POSIXct), and \code{DOLocationID}.}

\item{start_day_fitted_model}{A fitted tidymodels workflow (or any model
object with a \code{predict} method that returns a tibble containing a column
\code{.pred_class} with values "yes"/"no"). The model is used to classify
whether a trip start is high-value. It must have been trained on features
including \code{hvfhs_license_num}, \code{request_datetime}, \code{DOLocationID}, and
\code{daily_hourly_wage_mean} (the latter is set to 0 by this function).}

\item{valid_start_times_dt}{A data.table defining valid start hours. Must
have columns: \code{hour} (integer 0-23), \code{week_day} (integer 1-7, where
1 = Sunday), and \code{week_cycle} (integer 0-167, computed as
(week_day-1)*24 + hour).}
}
\value{
A modified data.table with the same structure as \code{trips_dt}, but with
\itemize{
\item \code{hvfhs_license_num} forced to \code{"HV0003"} (Uber),
\item \code{request_datetime} adjusted to the next valid start time for trips that
were not initially classified as high-value,
\item All trips guaranteed to be classified as high-value after adjustment
(asserted via \code{stopifnot}).
\item Auxiliary columns \code{daily_hourly_wage_mean} and \code{simulation_id} (added
internally) are removed before returning.
}
}
\description{
Applies a policy to adjust trip start times and enforce Uber as the taxi
company. Trips that are not predicted as high-value by the fitted model are
delayed to the next valid start time (hour/day combination) from a lookup
table. The function ensures that after adjustment all trips are classified
as high-value.
}
\details{
The function works as follows:
\enumerate{
\item Validates input types and required columns.
\item Makes a copy of \code{trips_dt} and sets \code{hvfhs_license_num = "HV0003"},
\code{daily_hourly_wage_mean = 0}, \code{simulation_id = 0} (the latter two are
required by the model but not used in final output).
\item Uses \code{stats::predict()} with \code{type = "class"} on the entire dataset to
identify trips classified as "no" (non-high-value).
\item For those trips, computes the current \code{week_cycle} from
\code{request_datetime}, then finds the next larger \code{week_cycle} from
\code{valid_start_times_dt} (allowing wrap‑around to the following week).
\item Updates \code{request_datetime} to the nearest hour floor plus the waiting
duration in hours.
\item Verifies that after adjustment all updated trips are now classified as
"yes". If not, an error is thrown.
\item Removes the temporary columns and returns the modified data.table.
}

The function assumes that the model was trained on data where a positive
classification corresponds to a high-value start (i.e., the policy that
leads to above‑baseline hourly earnings). The valid start times are
typically derived from the model's decision tree (e.g., hours and days where
the model predicts "yes").
}
\examples{
\dontrun{
# Assuming `fitted_workflow` and `valid_hours` are available
library(data.table)
trips <- data.table(
  hvfhs_license_num = c("HV0005", "HV0003"),
  request_datetime = as.POSIXct(c("2023-01-01 09:00:00", "2023-01-01 22:00:00")),
  DOLocationID = c("100", "200")
)
optimized_trips <- optimize_trip_start_time(trips, fitted_workflow, valid_hours)
}

}
